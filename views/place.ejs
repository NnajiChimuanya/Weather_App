<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Radio+Canada&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet"/>
    <link href="https://fonts.googleapis.com/css?family=Roboto:300,400,500,700&display=swap" rel="stylesheet"/>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/mdb-ui-kit/4.0.0/mdb.min.css" rel="stylesheet"/>
    <link rel="stylesheet" type="text/css" href="css/place.css">
    <title>Weather App</title>
</head>
<body>
 
 

  <div class="container-fluid">
    <div class="card mb-3" style="max-width: 540px;">
      <div class="row g-0">
        <div class="">
          <div id="map"></div>
        </div>
        
        <div class="">
          <div class="card-body">
            <h5 class="card-title"> <%= place.name %>, <%= place.sys.country %></h5>
            <p class="card-text"><small class="text-muted"><%= new Date().toUTCString() %></p>
            <h3 class="card-title"> <%= ((place.main.temp-32) / 1.8).toFixed(0) %>°C </h5>
            <h5 class="card-title"> Feels like :  <%= ((place.main.feels_like-32) / 1.8).toFixed(0) %>°C </h5>
            <h5 class="card-title"> Minimum Temp :  <%= ((place.main.temp_min-32) / 1.8).toFixed(0) %>°C </h5>
            <h5 class="card-title"> Maximum Temp :  <%= ((place.main.temp_max-32) / 1.8).toFixed(0) %>°C </h5>
            <h5 class="card-title"> Pressure :  <%= place.main.pressure %>hPa </h5>
            <h5 class="card-title"> Humidity :  <%= place.main.humidity %>% </h5>

             
            
          </div>
        </div>
      </div>
    </div>
  </div>

    

      


    <script>
      function initMap() {
          const uluru = { lat: parseInt('<%= JSON.stringify(place.coord.lat) %>'), lng: parseInt('<%= JSON.stringify(place.coord.lon) %>') };
          const map = new google.maps.Map(document.getElementById("map"), {
          zoom: 4,
          center: uluru,
        });
        const marker = new google.maps.Marker({
          position: uluru,
          map: map,
        });
      }
      window.initMap = initMap;
    </script>

    <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/mdb-ui-kit/4.0.0/mdb.min.js"></script>

    <!-- Google map api -->
    <script async defer src="https://maps.googleapis.com/maps/api/js?key=AIzaSyB1fsZR8PBAstlRYoHz3FHyGmK9y80nxok&callback=initMap"></script>

  </body>
</html>